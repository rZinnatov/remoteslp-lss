{
    "api": {
        "port": 0,
        "path": "",
        "_comment": "send requests to 'domain.com:<port>/<path>/...'"
    },
    "storage": {
        "url": "mongo url",
        "dbName": "mongo db name",
        "collectionName": "mongo collection name",
        "_comment": "where session are stored"
    }
}